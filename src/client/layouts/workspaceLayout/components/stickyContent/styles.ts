import { styled } from "@linaria/react";

const Container = styled.div`
  position: sticky;
  top: 0;

  display: flex;
  flex-direction: column;

  gap: 8px;
  padding: 16px 0;
`;

export { Container };
